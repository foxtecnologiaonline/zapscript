node_modules/
dist/
.next/
.env
.env.local
.sessions/
*.ogg
*.mp3
!apps/api/private/welcome.mp3
*.wav
.DS_Store

# Kits de marketing locais (não vão ao repositório)
marketing/kit-institucional-zapscript.md
marketing/kit-roberto-founder.md
coverage/

# Scripts temporários de setup
RUN-*.bat
execute-setup.js
setup-implementacao.ps1

# Logs
*.log
npm-debug.log*
.vercel

# Env files com segredos — NUNCA commitar
*.env
.env.*
!.env.example
!apps/monitor/.env.example
zapscript.env
zapscriptworker.env

# Scripts temporários / one-off com credenciais
q.js
scripts_temp/
scripts/migrate-full-db.mjs
scripts/migrate-supabase-users.mjs
scripts/send-migration-notice.mjs
scripts/send-password-reset.mjs

.env*

# Config local do Claude Code — acumula comandos aprovados com valores literais
# (tokens, senhas de banco, chaves de API) coladas em comandos ao longo do uso.
# NUNCA commitar; é pessoal/por-máquina, não faz sentido versionar mesmo sem o
# risco de segredo. Removido do histórico em 2026-08-22 (estava público no repo).
.claude/settings.local.json
