# O Prisma CLI (migrate/generate/studio) carrega o .env que estiver na MESMA
# pasta do schema.prisma — não lê apps/zapbox/.env (esse é só pro Next.js em
# runtime). Por isso existe uma cópia mínima aqui, só com a variável de banco.
ZAPBOX_DATABASE_URL="postgresql://user:pass@host:5432/zapbox"
